---

import "../../styles/global.css";
import "./Time.css"

const { datecreated } = Astro.props;

const rawDate = new Date(datecreated);
const formattedDate = rawDate.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });

---

<div class="TimeCard">
    📆 {formattedDate}
</div>